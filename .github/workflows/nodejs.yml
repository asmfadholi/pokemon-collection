name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.13]

    steps:
    - run: ls
    - run: npm install
    - run: npm run build
    - run: npm test
      env:
        CI: true
